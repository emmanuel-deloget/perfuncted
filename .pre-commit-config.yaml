repos:
- repo: local
  hooks:
    - id: run-ci
      name: Run quick checks
      entry: bash -lc "gofmt -l . && go vet ./..."
      language: script
      always_run: true
      pass_filenames: false
      stages: [commit, push]
